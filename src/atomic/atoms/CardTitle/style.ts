import styled from 'styled-components'

export const Title = styled.h2`
  color: ${({ theme }) => theme.colors.black};
  align-self: center;
  font-size: 16px;
  margin: 0;
  text-align: center;
  padding: 6px 0px;
`
