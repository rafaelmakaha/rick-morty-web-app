import styled from 'styled-components'

export const Title = styled.h2`
  color: ${({ theme }) => theme.colors.black};
  align-self: center;
  font-size: 16px;
  margin: auto;
  text-align: center;
  padding: 6px;
`
